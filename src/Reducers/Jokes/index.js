let defaultState = ['SOME JOKE']

const JokesReducer = (state = defaultState, action) => {
    switch (action.type) {
        case "SAVE":
            
            return(
                ...state,
                return state 
            )
    
        default:
            break;
    }

}

export default JokesReducer;